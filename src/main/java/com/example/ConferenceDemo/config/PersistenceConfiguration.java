package com.example.ConferenceDemo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class PersistenceConfiguration {   // it returns Bean context
//    @Bean
//    public DataSource datasource(){
//        DataSourceBuilder builder = DataSourceBuilder.create();
//        builder.url(System.getenv("DB_URL"));
//        builder.username(System.getenv("DB_USER"));
//        builder.password(System.getenv("DB_PW"));
//        System.out.println(builder);
//        return builder.build();
//    }
}
